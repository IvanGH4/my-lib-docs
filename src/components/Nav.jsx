import React from 'react';
import styles from './Global.module.css';

const Nav = () => {
  return (
    <nav className={styles.nav}>
      <p>I did not really needed a navbar...</p>
    </nav>
  );
};

export default Nav;
